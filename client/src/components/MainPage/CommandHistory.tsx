interface CommandHistoryProps {
  className: string;
}

function CommandHistory({ className }: CommandHistoryProps) {
  return <div className={className}>Command History</div>;
}

export default CommandHistory;
